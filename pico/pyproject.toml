[project]
name = "pico_weather_hardware"
version = "0.1.0"
description = "MicroPython firmware code"
requires-python = ">=3.4" # MicroPython targets older core syntax specs

[tool.pyright]
include = ["."]
exclude = ["typings"]
stubPath = "typings"
extraPaths = ["lib"]
typeCheckingMode = "standard"
